window.TM=window.TM||{};TM.Profile=(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function open(){const user=await TM.Auth.currentUser();if(!user)return;const content=document.querySelector('#page-content');document.querySelectorAll('.nav button').forEach(b=>b.classList.remove('active'));content.innerHTML=`<h1>My Profile</h1><div class="profile-grid"><div class="card"><h2>Profile</h2><div id="profile-msg" class="hidden"></div><form id="profile-form" class="form-grid"><label>Username<input value="${esc(user.username)}" disabled></label><label>Display name<input name="displayName" value="${esc(user.displayName||'')}" maxlength="160"></label><label>Email<input name="email" type="email" value="${esc(user.email||'')}" maxlength="254"></label><button>Save profile</button></form></div><div class="card"><h2>Change password</h2><div id="password-msg" class="hidden"></div><form id="profile-password-form" class="form-grid"><label>Current password<input name="old" type="password" required autocomplete="current-password"></label><label>New password<input name="newp" type="password" minlength="12" required autocomplete="new-password"></label><label>Confirm new password<input name="confirm" type="password" minlength="12" required autocomplete="new-password"></label><button>Change password</button></form><p class="muted">Minimum 12 characters. Your current password is required.</p></div></div>`;document.querySelector('#profile-form').onsubmit=async e=>{e.preventDefault();const form=e.currentTarget,f=new FormData(form),m=document.querySelector('#profile-msg');try{const fresh=await TM.DB.get('users',user.id);fresh.displayName=String(f.get('displayName')||'').trim();fresh.email=String(f.get('email')||'').trim();await TM.DB.put('users',fresh);await TM.DB.audit('PROFILE_UPDATED','user',fresh.id,{displayNameChanged:fresh.displayName!==user.displayName,emailChanged:fresh.email!==user.email});const label=document.querySelector('#current-user');if(label)label.textContent=fresh.displayName||fresh.username;m.textContent='Profile saved.';m.className='success'}catch(x){m.textContent=x.message;m.className='error'}};document.querySelector('#profile-password-form').onsubmit=async e=>{e.preventDefault();const form=e.currentTarget,f=new FormData(form),m=document.querySelector('#password-msg');try{await TM.Auth.changePassword(user.id,f.get('old'),f.get('newp'),f.get('confirm'));form.reset();m.textContent='Password changed successfully.';m.className='success'}catch(x){m.textContent=x.message;m.className='error'}}}function bind(){const b=document.querySelector('#profile-button');if(b)b.onclick=open}document.addEventListener('DOMContentLoaded',bind);return{open,bind}})();
